@page
@model RazorCoursework.Pages.SearchReviewsModel
@{
}

@if (Model.pagesCount > 1)
{
    <partial name="_SearchReviewsPagingPartial" />
    <hr />
}
<partial name="_ShowReviewsPartial" />
@if (Model.pagesCount > 1)
{
    <hr />
    <partial name="_SearchReviewsPagingPartial" />
}
